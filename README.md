# sentiment-analysis-transformer
Sentiment analysis using a pre-trained Transformer model (DistilBERT) with HuggingFace pipelines. Includes custom dataset, predictions, evaluation metrics, and visualizations.
